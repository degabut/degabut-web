import { VideosGrid } from "./grid.component";
import { VideosList } from "./list.component";
import { SortableVideosList } from "./sortable-list.component";

export const Videos = {
	List: VideosList,
	SortableList: SortableVideosList,
	Grid: VideosGrid,
};
