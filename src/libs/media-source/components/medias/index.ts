import { MediaSourcesGrid } from "./grid.component";
import { MediaSourcesList } from "./list.component";
import { SortableMediaSourcesList } from "./sortable-list.component";

export const MediaSources = {
	List: MediaSourcesList,
	SortableList: SortableMediaSourcesList,
	Grid: MediaSourcesGrid,
};
